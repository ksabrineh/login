import Login from '@/components/LoginsPages/Login';
import React from 'react';

const Page = () => {
    return (
        <div className="w-screen flex justify-center items-center h-screen">
            <Login />
        </div>
    );
};

export default Page;